node_modules/
dist/
.vite/
coverage/

.env
.env.local
*.local

pb_data/
pocketbase
pocketbase.exe

.DS_Store
Thumbs.db
*.log

# scratch issues ARE tracked - see docs/agents/issue-tracker.md
*.tsbuildinfo
.turbo/

# --- extraction pipeline (see extraction/README.md) ---
# The 7.7 GB scan corpus. Never committed; it is an input, not an asset.
raw_konkour_files/
# Regenerable local artifacts: OCR text cache and rendered page PNGs.
extraction/cache/

# Raw exam papers: 2241 files / ~7.9 GB. Far past what Git LFS can hold.
# Tracked by sources/manifest.md instead. See ADR-0006.
raw_konkour_files/

# Python bytecode from extraction/scripts
__pycache__/
*.pyc

# --- application scaffold (ticket dev-foundation/01) ---
# vite-plugin-pwa's dev-mode service worker output.
dev-dist/
# Content packages are built by `pnpm content:build`, never committed.
apps/web/public/content/
server/content/
# Playwright output.
test-results/
playwright-report/
# The PocketBase binary CI downloads for the e2e job.
server/.pb/
# Scratch git worktrees the agent harness creates. The tracked .claude/ files stay tracked.
.claude/worktrees/
