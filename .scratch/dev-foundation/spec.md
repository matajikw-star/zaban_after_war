# dev-foundation — Phase 1 of the implementation plan

Spec: `docs/spec/what.md` §3 (layout), §4 (stack), §5 (engine), §16.1 (engine tests), §17
(conventions). Plan: `docs/plan/implementation-plan.md` → Phase 1.

Goal: the workspace scaffolds exist, the engine is rewritten to the current contracts and
exhaustively tested, the simulator runs, and CI is green — before any UI.

Tickets are numbered in `issues/`. Each ends with `what.md` status marks flipped for what it
shipped.
