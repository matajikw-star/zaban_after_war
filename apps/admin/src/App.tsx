/**
 * The owner's dashboard (what.md §11.2). It is seen by one person, so its copy is English and
 * it has no strings file. Empty until the dashboard ticket.
 */
export function App() {
  return (
    <main className="page">
      <h1>Admin</h1>
    </main>
  );
}
