/** Placeholder for the checkout screen (`what.md` §7.8). Built by a later ticket. */

import { strings } from '../../strings.ts';
import { PlaceholderScreen } from '../layout/Placeholder.tsx';

export function Checkout() {
  return <PlaceholderScreen title={strings.screens.checkout} />;
}
