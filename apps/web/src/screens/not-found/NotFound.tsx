/** Placeholder for the notFound screen (`what.md` §7.8). Built by a later ticket. */

import { strings } from '../../strings.ts';
import { PlaceholderScreen } from '../layout/Placeholder.tsx';

export function NotFound() {
  return <PlaceholderScreen title={strings.screens.notFound} />;
}
