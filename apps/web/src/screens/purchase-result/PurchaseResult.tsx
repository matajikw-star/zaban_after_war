/** Placeholder for the purchaseResult screen (`what.md` §7.8). Built by a later ticket. */

import { strings } from '../../strings.ts';
import { PlaceholderScreen } from '../layout/Placeholder.tsx';

export function PurchaseResult() {
  return <PlaceholderScreen title={strings.screens.purchaseResult} />;
}
