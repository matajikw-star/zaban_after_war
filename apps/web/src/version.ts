// Injected by Vite's `define` (see vite.config.ts). Every error report carries both, so a
// stack trace can be matched to the sourcemaps uploaded for that build (what.md §10.1, §14.4).

export const APP_VERSION: string = __APP_VERSION__;
export const BUILD_SHA: string = __BUILD_SHA__;
