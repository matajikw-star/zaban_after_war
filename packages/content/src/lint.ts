/**
 * The `lint` operation over content/. Checklist: docs/plan/content-pipeline.md.
 *
 * Stub until M2 — it exits 0 so CI is honest about there being no content yet,
 * rather than reporting a clean lint that never ran.
 */
console.log('content lint: no lexicon yet (M2). Checklist: docs/plan/content-pipeline.md');
