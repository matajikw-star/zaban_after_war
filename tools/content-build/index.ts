#!/usr/bin/env node
// Builds free.json and paid.json plus the manifest from content/ (what.md §6.2).
// See docs/spec/what.md for the behaviour this must eventually have.

console.error('content:build: not implemented');
process.exit(1);
