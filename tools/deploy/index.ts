#!/usr/bin/env node
// Builds, ships and health-checks a release on the VPS (what.md §14.4).
// See docs/spec/what.md for the behaviour this must eventually have.

console.error('deploy: not implemented');
process.exit(1);
