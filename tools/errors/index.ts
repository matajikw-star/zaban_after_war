#!/usr/bin/env node
// Reads client_errors from the server and groups them into a ranked list (what.md §10.3).
// See docs/spec/what.md for the behaviour this must eventually have.

console.error('errors: not implemented');
process.exit(1);
