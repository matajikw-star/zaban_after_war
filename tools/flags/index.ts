#!/usr/bin/env node
// Lists and resolves user-submitted word flags (what.md §10.3).
// See docs/spec/what.md for the behaviour this must eventually have.

console.error('flags: not implemented');
process.exit(1);
