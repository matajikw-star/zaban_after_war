#!/usr/bin/env node
// Tails and filters the server's structured logs by request id (what.md §10.2, §10.3).
// See docs/spec/what.md for the behaviour this must eventually have.

console.error('logs: not implemented');
process.exit(1);
