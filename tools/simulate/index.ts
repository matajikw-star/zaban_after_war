#!/usr/bin/env node
// Schedule simulator: runs a synthetic user through the engine and prints the curve (what.md §5.7).
// See docs/spec/what.md for the behaviour this must eventually have.

console.error('simulate: not implemented');
process.exit(1);
